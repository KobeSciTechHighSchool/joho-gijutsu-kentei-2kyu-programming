#include <stdio.h>
int main(void){
  int ①;
  int k, m, n;
  
  for( k = 0; k < 5; k++ ){
    printf("d[%d]の値？", k);
    scanf("%d", ②);
  }
  printf("\n");
  for( m = 0; m < ③; m++ ){
    for( n = 0; n < ④; n++ ){
      printf("*");
    }
    ⑤
  }
  
  return 0;
}
