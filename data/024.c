#include <stdio.h>
int main(void){
  int dat[10] = { 7, 13, 18, 22, 27, 32, 38, 44, 49, 51 };
  int j, key;
  
  printf("検索データ");
  scanf("%d", ① );
  j = 0;
  while( j < ② ){
    if( key == ③ ){
      break;
    }
    ④;
  }
  if( j ⑤ 10 ){
    printf("検索データは%d番目\n", j + 1);
  }
  else{
    printf("検索データなし\n");
  }
  
  return 0;
}
