#include <stdio.h>
int main(void){
  int bangou[10] = {101, 102, 103, 104, 105, 201, 202, 203, 204, 205 };
  int tokuten[10] = {71, 52, 63, 94, 85, 66, 77, 48, 69, 100 };
  int key, m;
  
  printf("受験番号=");
  scanf("%d", &key);
  for( m = 0; m <= ①; ② ){
    if( key == ③ ){
      break;
    }
  }
  if( m > ④ ){
    printf("該当なし\n");
  }
  else{
    printf("得点=%d\n", ⑤);
  }

  return 0;
}
